// Firebase configuration - Replace with your actual Firebase project credentials
// Get these from your Firebase Console -> Project Settings

export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID"
};

// To enable Google Sign-In:
// 1. Go to Firebase Console -> Authentication -> Sign-in method
// 2. Enable Google sign-in provider
// 3. In Google Cloud Console, create OAuth 2.0 credentials and add the client ID
